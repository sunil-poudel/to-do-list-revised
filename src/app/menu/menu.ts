import {Component, EventEmitter, Output, inject} from '@angular/core';

@Component({
  selector: 'app-menu',
  imports: [
  ],
  templateUrl: './menu.html',
  styleUrl: './menu.css'
})
export class Menu {
  onClickAdd(){

  }
}
